pre-commit:
  commands:
    nodejs:
      glob: "backend_nodejs/**/*.{ts,tsx,js,jsx}"
      run: |
        cd backend_nodejs
        npm install
        npm run check
        npm test
    golang:
      glob: "backend_golang/**/*.go"
      run: |
        cd backend_golang
        go mod tidy
        go fmt ./...
        go vet ./...
        go test ./...
      